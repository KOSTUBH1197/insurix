// Real schema (users, policies, claims, documents, extractions,
// settlements, deductions, rule sets — insurix-build-spec.md section 6)
// lands in build stage 4. Intentionally empty until then.
export {};
