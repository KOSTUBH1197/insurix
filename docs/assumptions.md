# Assumptions

Ambiguities in the build spec, especially regulatory ones, that were resolved
by implementing a configurable version rather than guessing a fixed answer.
Each entry needs verification against a real policy document, regulation, or
legal opinion before launch.

## Open

- **Document retention period.** Section 10 requires "minimum retention with
  an automatic deletion job" but specifies no duration. Not yet implemented
  or assumed — needs a DPDPA-compliant number before the retention job
  (referenced in section 6) is built.
